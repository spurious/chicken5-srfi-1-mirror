;; -*- Scheme -*-

(compile srfi-1.scm -O3 -d1 -sJ)
(compile srfi-1.scm -O3 -d1 -c -no-module-registration -unit srfi-1)
(compile srfi-1.import.scm -O3 -d0 -s)

(install-extension
 'srfi-1
 '("srfi-1.so" "srfi-1.import.so" "srfi-1.o" "srfi-1.types")
 '((version "1.1")))
